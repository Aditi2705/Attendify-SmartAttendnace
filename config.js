// Global frontend config for development
// Set API_BASE here and it will be available to all pages via window.API_BASE
// Use HTTP for dev (dotnet run uses HTTP on localhost)
window.API_BASE = window.API_BASE || 'http://localhost:5014/api';
